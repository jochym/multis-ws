#!/bin/bash
# start script for repo2docker/mybinder
# Runs at the start of each user session

# Set up environment variables for ABINIT
export GPAW_SETUP_PATH=/opt/conda/share/gpaw-setups 2>/dev/null || true

# Add scripts to PATH
export PATH=$PATH:$(pwd)/scripts

# Execute the original command
exec "$@"